/**
 * Limites base para configurar sesiones de mindfulness.
 */
export const mindfulnessConfig = {
	minDurationMinutes: 1,
	maxDurationMinutes: 20,
	minScaleValue: 0,
	maxScaleValue: 10,
} as const;
